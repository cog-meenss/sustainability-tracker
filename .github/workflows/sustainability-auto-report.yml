name: Automated Sustainability Analysis & Report Generation

on:
  push:
    branches: [ main, develop ]
    paths:
      - '**/*.py'
      - '**/*.js'
      - '**/*.ts'
      - '**/*.jsx'
      - '**/*.tsx'
      - '**/*.java'
      - '**/*.cpp'
      - '**/*.c'
      - '**/*.cs'
      - '**/*.go'
      - '**/*.rs'
      - '**/*.php'
      - '**/*.rb'
      - 'package.json'
      - 'requirements.txt'
      - 'Dockerfile'
  pull_request:
    branches: [ main ]
    paths:
      - '**/*.py'
      - '**/*.js'
      - '**/*.ts'
      - '**/*.jsx'
      - '**/*.tsx'
  workflow_dispatch:
    inputs:
      analysis_path:
        description: 'Path to analyze (default: entire repo)'
        required: false
        default: '.'
      output_name:
        description: 'Custom output filename'
        required: false
        default: 'sustainability-report'

env:
  PYTHON_VERSION: '3.9'
  REPORT_PATH: 'sustainability-reports'

jobs:
  sustainability-analysis:
    name: 🌱 Generate Sustainability Report
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pages: write
      id-token: write
      actions: read
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        
    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests psutil
        # Install optional Flask dependencies for API features
        pip install flask flask-cors || echo "⚠️ Flask optional dependencies not installed"
        
    - name: 🔍 Verify Sustainability Evaluator
      run: |
        if [ ! -f "sustainability_evaluator.py" ]; then
          echo "❌ sustainability_evaluator.py not found in repository"
          exit 1
        fi
        echo "✅ Sustainability evaluator found"
        
    - name: 📊 Create Reports Directory
      run: |
        mkdir -p ${{ env.REPORT_PATH }}
        echo "📁 Created reports directory: ${{ env.REPORT_PATH }}"
        
    - name: 🌱 Run Sustainability Analysis
      id: analysis
      run: |
        echo "🚀 Starting comprehensive sustainability analysis..."
        
        # Set analysis path (from workflow input or default)
        ANALYSIS_PATH="${{ github.event.inputs.analysis_path || '.' }}"
        
        # Set output filename with timestamp
        TIMESTAMP=$(date +%Y%m%d_%H%M%S)
        COMMIT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
        
        if [ "${{ github.event.inputs.output_name }}" != "" ]; then
          OUTPUT_BASE="${{ github.event.inputs.output_name }}"
        else
          OUTPUT_BASE="sustainability-report-${COMMIT_SHA}-${TIMESTAMP}"
        fi
        
        HTML_OUTPUT="${{ env.REPORT_PATH }}/${OUTPUT_BASE}.html"
        JSON_OUTPUT="${{ env.REPORT_PATH }}/${OUTPUT_BASE}.json"
        
        # Run sustainability analysis with both HTML and JSON outputs
        python3 sustainability_evaluator.py \
          --path "${ANALYSIS_PATH}" \
          --format both \
          --output "${{ env.REPORT_PATH }}/${OUTPUT_BASE}"
        
        # Verify outputs were created
        if [ -f "$HTML_OUTPUT" ]; then
          echo "✅ HTML report generated: $HTML_OUTPUT"
          echo "html_report=$HTML_OUTPUT" >> $GITHUB_OUTPUT
        else
          echo "❌ HTML report generation failed"
          exit 1
        fi
        
        if [ -f "$JSON_OUTPUT" ]; then
          echo "✅ JSON report generated: $JSON_OUTPUT"
          echo "json_report=$JSON_OUTPUT" >> $GITHUB_OUTPUT
        fi
        
        # Extract key metrics for summary
        OVERALL_SCORE=$(python3 -c "
import json
try:
    with open('$JSON_OUTPUT') as f:
        data = json.load(f)
        print(f\"{data['sustainability_metrics']['overall_score']:.1f}\")
except: print('N/A')
        ")
        
        ENERGY_EFFICIENCY=$(python3 -c "
import json
try:
    with open('$JSON_OUTPUT') as f:
        data = json.load(f)
        print(f\"{data['sustainability_metrics']['energy_efficiency']:.1f}\")
except: print('N/A')
        ")
        
        GREEN_SCORE=$(python3 -c "
import json
try:
    with open('$JSON_OUTPUT') as f:
        data = json.load(f)
        print(f\"{data['sustainability_metrics'].get('green_coding_score', 0):.1f}\")
except: print('N/A')
        ")
        
        RECOMMENDATIONS_COUNT=$(python3 -c "
import json
try:
    with open('$JSON_OUTPUT') as f:
        data = json.load(f)
        print(len(data.get('recommendations', [])))
except: print('0')
        ")
        
        # Set outputs for later steps
        echo "overall_score=$OVERALL_SCORE" >> $GITHUB_OUTPUT
        echo "energy_efficiency=$ENERGY_EFFICIENCY" >> $GITHUB_OUTPUT
        echo "green_score=$GREEN_SCORE" >> $GITHUB_OUTPUT
        echo "recommendations_count=$RECOMMENDATIONS_COUNT" >> $GITHUB_OUTPUT
        echo "output_base=$OUTPUT_BASE" >> $GITHUB_OUTPUT
        
    - name: 📈 Generate Analysis Summary
      id: summary
      run: |
        echo "📊 Generating analysis summary..."
        
        # Create a comprehensive summary file
        SUMMARY_FILE="${{ env.REPORT_PATH }}/analysis-summary.md"
        
        cat > "$SUMMARY_FILE" << EOF
        # 🌱 Sustainability Analysis Report
        
        **Generated:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')  
        **Commit:** \`${{ github.sha }}\`  
        **Branch:** \`${{ github.ref_name }}\`  
        **Triggered by:** ${{ github.event_name }}
        
        ## 📊 Key Metrics
        
        | Metric | Score | Status |
        |--------|-------|--------|
        | **Overall Sustainability** | ${{ steps.analysis.outputs.overall_score }}/100 | $([ $(echo "${{ steps.analysis.outputs.overall_score }} >= 80" | bc -l) -eq 1 ] && echo "🟢 Excellent" || [ $(echo "${{ steps.analysis.outputs.overall_score }} >= 60" | bc -l) -eq 1 ] && echo "🟡 Good" || echo "🔴 Needs Improvement") |
        | **Energy Efficiency** | ${{ steps.analysis.outputs.energy_efficiency }}/100 | $([ $(echo "${{ steps.analysis.outputs.energy_efficiency }} >= 80" | bc -l) -eq 1 ] && echo "🟢 Excellent" || [ $(echo "${{ steps.analysis.outputs.energy_efficiency }} >= 60" | bc -l) -eq 1 ] && echo "🟡 Good" || echo "🔴 Needs Improvement") |
        | **Green Coding Score** | ${{ steps.analysis.outputs.green_score }}/100 | $([ $(echo "${{ steps.analysis.outputs.green_score }} >= 80" | bc -l) -eq 1 ] && echo "🟢 Excellent" || [ $(echo "${{ steps.analysis.outputs.green_score }} >= 60" | bc -l) -eq 1 ] && echo "🟡 Good" || echo "🔴 Needs Improvement") |
        | **Recommendations** | ${{ steps.analysis.outputs.recommendations_count }} items | $([ ${{ steps.analysis.outputs.recommendations_count }} -eq 0 ] && echo "🟢 No Issues" || [ ${{ steps.analysis.outputs.recommendations_count }} -le 5 ] && echo "🟡 Minor Issues" || echo "🔴 Major Issues") |
        
        ## 📁 Generated Reports
        
        - 🎯 **Interactive Dashboard:** [\`${{ steps.analysis.outputs.output_base }}.html\`](${{ steps.analysis.outputs.html_report }})
        - 📋 **JSON Data:** [\`${{ steps.analysis.outputs.output_base }}.json\`](${{ steps.analysis.outputs.json_report }})
        
        ## 🎯 Quick Actions
        
        - View the interactive dashboard for detailed analysis
        - Check file-specific recommendations with line numbers
        - Review green coding suggestions for energy efficiency
        - Monitor carbon footprint metrics and projections
        
        ---
        *Generated by Automated Sustainability Analysis Workflow*
        EOF
        
        echo "summary_file=$SUMMARY_FILE" >> $GITHUB_OUTPUT
        
    - name: 🏗️ Setup GitHub Pages (if enabled)
      uses: actions/configure-pages@v3
      if: github.ref == 'refs/heads/main'
      continue-on-error: true
      
    - name: 📤 Upload Reports as Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: sustainability-reports-${{ github.sha }}
        path: |
          ${{ env.REPORT_PATH }}/*.html
          ${{ env.REPORT_PATH }}/*.json
          ${{ env.REPORT_PATH }}/*.md
        retention-days: 30
        
    - name: 📝 Commit Reports to Repository
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Add generated reports
        git add ${{ env.REPORT_PATH }}/
        
        # Check if there are changes to commit
        if git diff --staged --quiet; then
          echo "📝 No new reports to commit"
        else
          git commit -m "🌱 Auto-generated sustainability reports
          
          - Overall Score: ${{ steps.analysis.outputs.overall_score }}/100
          - Energy Efficiency: ${{ steps.analysis.outputs.energy_efficiency }}/100  
          - Green Coding: ${{ steps.analysis.outputs.green_score }}/100
          - Recommendations: ${{ steps.analysis.outputs.recommendations_count }} items
          
          Generated from commit: ${{ github.sha }}
          Analysis triggered by: ${{ github.event_name }}"
          
          git push
          echo "✅ Reports committed and pushed to repository"
        fi
        
    - name: 💬 Comment PR with Results
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          const summaryPath = '${{ steps.summary.outputs.summary_file }}';
          
          if (fs.existsSync(summaryPath)) {
            const summary = fs.readFileSync(summaryPath, 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🌱 Sustainability Analysis Results\n\n${summary}\n\n---\n\n**Download Reports:** [Sustainability Reports Artifact](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})`
            });
          }
          
    - name: 🚨 Quality Gates Check
      run: |
        echo "🔍 Checking sustainability quality gates..."
        
        OVERALL_SCORE=${{ steps.analysis.outputs.overall_score }}
        MIN_SCORE=50  # Minimum acceptable sustainability score
        
        if (( $(echo "$OVERALL_SCORE < $MIN_SCORE" | bc -l) )); then
          echo "❌ Sustainability quality gate FAILED"
          echo "   Current score: $OVERALL_SCORE"  
          echo "   Required minimum: $MIN_SCORE"
          echo "   Please review the generated recommendations"
          exit 1
        else
          echo "✅ Sustainability quality gate PASSED"
          echo "   Current score: $OVERALL_SCORE (>= $MIN_SCORE)"
        fi
        
    - name: 📊 Update Job Summary
      if: always()
      run: |
        cat >> $GITHUB_STEP_SUMMARY << EOF
        # 🌱 Sustainability Analysis Complete
        
        ## Results Overview
        - **Overall Score:** ${{ steps.analysis.outputs.overall_score }}/100
        - **Energy Efficiency:** ${{ steps.analysis.outputs.energy_efficiency }}/100
        - **Green Coding:** ${{ steps.analysis.outputs.green_score }}/100
        - **Recommendations:** ${{ steps.analysis.outputs.recommendations_count }} items
        
        ## Generated Artifacts
        - Interactive HTML Dashboard: \`${{ steps.analysis.outputs.output_base }}.html\`
        - Detailed JSON Report: \`${{ steps.analysis.outputs.output_base }}.json\`
        - Analysis Summary: \`analysis-summary.md\`
        
        ## 🔗 Quick Links
        - [📥 Download Reports Artifact](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
        - [📊 View Workflow Details](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
        
        ---
        *Analysis completed at $(date -u '+%Y-%m-%d %H:%M:%S UTC')*
        EOF