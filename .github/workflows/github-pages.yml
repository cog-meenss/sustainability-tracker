# DEPRECATED: This workflow has been consolidated into consolidated-sustainability.yml
# Keeping this file for reference but disabling it to avoid conflicts

name: Deploy to GitHub Pages (DISABLED)

on:
  # Disabled - removing all triggers to prevent conflicts
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false
        default: 'true'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-disabled"
  cancel-in-progress: false

jobs:
  disabled:
    if: false  # This job will never run
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow has been consolidated into consolidated-sustainability.yml"